{
  "configurations": [
    {
      "name": "Test",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bazel-bin/test",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "bazel build test",
      "sourceMap": {
        "/proc/self/cwd" : "${workspaceFolder}"
      }
    }
  ],
  "version": "0.2.0"
}