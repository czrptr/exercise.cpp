load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
  name = "serde",
  srcs = glob(["*.cc"], allow_empty = True),
  hdrs = glob(["*.hh"], allow_empty = True),
  deps = [
    "//lib:lib",
    "@range-v3//:range-v3",
    "@fmt//:fmt",
  ],
  visibility = ["//visibility:public"],
)
