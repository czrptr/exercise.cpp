load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
  name = "serde",
  srcs = glob(["**/*.cc"]),
  hdrs = glob(["**/*.hh"]),
  deps = [
    "//lib:lib",
    "@range-v3//:range-v3",
    "@fmt//:fmt",
  ],
  visibility = ["//visibility:public"],
)
